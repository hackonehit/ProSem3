@model Online_Help_Desk.Models.Enduser

@{
    ViewBag.Title = "ChangePassword";
    Layout = "~/Views/Shareds/_LayoutIndexEnduser.cshtml";
}

<h2>ChangePassword</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Enduser</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.EUUsername)
        @Html.HiddenFor(model=>model.EUPassword)
        <div class="form-group">
            @Html.LabelFor(model => model.EUPassword, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.EUPassword, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.EUPassword, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.EUPassword, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
               <input type="password" value="abc" name="password"/>
                @Html.ValidationMessageFor(model => model.EUPassword, "", new { @class = "text-danger" })
            </div>
        </div>

        

       

        
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
